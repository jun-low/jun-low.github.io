---
import { Picture } from '@astrojs/image/components';
import bgImage from '../../public/sky-bg.jpg';
const widths = [450, 800, 1200];
const sizes = '100vw';
---

<Picture
	class="object-cover w-full h-full"
	src={bgImage}
	widths={widths}
	sizes={sizes}
	alt="Sky background with clouds"
/>
